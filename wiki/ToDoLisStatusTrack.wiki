#summary One-sentence summary of this page.

= Introduction =

to list and track the todo list


= Details =

==1. complete==

  * to generate execl 03/07 or 10   
  * path concat: linux/windows, if contain \.  
  * support module/sheet in the test case  
  * format pom.xml to match central maven requirements 
  * sort by case id  
  * support html generated  
  * fix the issue:html first module first case title is bold.


==2. doing==

  * logger improve(if debugable and format,etc)  


==3. todo==
  * eclipse plugin if need   
  * private static final String DEFAULT_EXCEL_NAME_FORMAT = "TestCase_%s%s"; to use %n date, and %s%s use some value.  
  * parameter validation  
  * add into meta-info service  
  * how to use it in java apt  
  * fix the issue: when no case, no html, so use publish html plugin build fail.


 
  