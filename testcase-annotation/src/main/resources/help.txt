1 add followed configure into pom.xml
	<dependencies>
		<dependency>
			<groupId>com.googlecode.testcase-annotation</groupId>
			<artifactId>testcase-annotation</artifactId>
			<version>1.0.1</version>
		</dependency>
 	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>apt-maven-plugin</artifactId>
				<version>1.0-alpha-5</version>
				<configuration>
					<factory>com.googlecode.testcase.annotation.AnnotationProcessFactoryImpl</factory>
<!-- 				<options>
 						<option>fileName=TestCase.xlsx</option>
						<option>excelType=xlsx</option>
 					</options>
 			 		<outputDirectory>C:\testapt</outputDirectory>  -->
 				</configuration>
 				<!-- 				<executions>
					<execution>
						<phase>test</phase>
						<goals>
							<goal>process</goal>
						</goals>
					</execution>
				</executions> -->
			</plugin>
		</plugins>
	</build>

2  run "mvn apt:process" to handle src/main/java or "mvn apt:test-process" to handle src/test/java.
detailed info can refer to:
http://mojo.codehaus.org/apt-maven-plugin/

3 if customized the lifecycle in maven: add into <plugin></plugin>

mvn test

add 			<!-- <executions>
					<executions>
					<execution>
						<phase>test</phase>
						<goals>
							<goal>process</goal>
							<goal>test-process</goal>
						</goals>
					</execution>
				</executions> -->
